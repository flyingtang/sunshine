import 'package:flutter/material.dart';

class DetailHeader extends StatelessWidget {
  // DetailHeader({this.title, this.createDate, this.authod});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return null;
  }
}
